function UploadAnalyzer() {
  return (
    <div className="card">
      <h2>Resume Upload Analyzer</h2>
      <input type="file" />
      <button>Analyze Resume</button>
      <p>Basic file upload UI (backend logic can be added later)</p>
    </div>
  );
}

export default UploadAnalyzer;